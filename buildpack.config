# Configuración del buildpack para optimizar memoria
[build]
  cache_directories = ["node_modules", ".npm-cache"]

[build.environment]
  NODE_OPTIONS = "--max-old-space-size=512"
  NPM_CONFIG_PRODUCTION = "true"
  NPM_CONFIG_CACHE = "/tmp/npm-cache"
  UV_THREADPOOL_SIZE = "4"
